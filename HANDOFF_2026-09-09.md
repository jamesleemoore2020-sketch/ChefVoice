# ChefVoice — engineering handoff, 2026-09-09

## 1. TASK
Instrument product analytics, ship launch-access entitlements (no Play Billing), and fix the parser defects a real nachos capture exposed.

## 2. TOUCHED
All uncommitted. Nothing committed this session.
- `analytics/ChefAnalytics.kt` (new) — event vocabulary + emitters; no-op without Firebase, never throws.
- `ChefVoiceApplication.kt` — `onCreate` → `ChefAnalytics.initialize`.
- `ui/ChefAppState.kt` — analytics at paywall/Second Pass; `showPaywall`/`dismissPaywall` are now the only entrance/exit.
- `ui/ChefVoiceApp.kt` — analytics call sites; complimentary-vs-subscriber copy; `remainingLabel`.
- `model/Models.kt` — `ProEntitlement` source constants + `isFounding`/`isPromo`/`isComplimentary`/`daysRemaining`; `FoundingAccess`.
- `voice/CookingSessionParser.kt` — 4 changes: determiner-gated `need`, yield-sentence suppression, back-reference rejection, declaration-not-continuation guard.
- `shared/golden-cooking-corpus.tsv` +3 rows; `GoldenCookingCorpusTest.kt` row guard 52→55 + nachos fixture test.
- `billing/functions/{index.js,package.json}`, `billing/launch-access.test.js`, `DEPLOY_BILLING.cmd`, `RUN_BILLING_GATES.cmd` (all new).
- `firebase.json` — `functions` is now an array of two codebases.
- `firestore.rules` — `config/{configId}` closed both ways. **Committed, not deployed.**
- `rules-tests/firestore-rules.test.js` +4 tests. `CLAUDE.md`, `BUILD_STATUS.md`, 3 `*_0.11.0.md` writeups.
- Pre-existing, unrelated, uncommitted: `AndroidManifest.xml`, `ChefVoiceForegroundService.kt`.

## 3. DECISIONS
- No custom `install` event — Firebase logs `first_open` automatically with attribution.
- `recipeCompleted` counts, not flags, so milestones can't double-fire.
- Version NOT bumped: stays 0.10.6 / code 58; four `versionCode 58` pins in `notifications/*.test.js`.
- Founding = flat 730 days from **grant**; promo = 90 days from **signup** (backfill would shorten old accounts).
- Kill switch: soft (default) stops new grants; `revokeActive:true` also expires promos. Founding and `play` never revoked.
- Absent `config/monetization` = promo running, so a failed console edit can't silently disable it.
- Backfill sorts in memory — `orderBy("createdAt")` drops docs missing the field.
- Revoke sweep avoids collection-group queries so it needs no new index.

## 4. RULED OUT
- **`need` in `narratedActionBoundary`** — shared with step building; removing it alone does not fix the window merge, and step pollution persists either way.
- **Splitting declarations inside `extractUnmeasuredIngredients`** — produced garbage ingredients `You're`, `Going to`.
- **Corpus row to guard the step regression** — the corpus asserts *required* substrings only; it cannot express "step must not contain X". Use a fixture test.
- **Second Pass as the defect** — verified working; it flagged 6+3 issues correctly. Both passes share the parser, so shared defects read as "confirmed".
- **Firestore reads via `curl` + `gcloud` token** — blocked by the sandbox classifier.
- **`firebase functions:shell` piped non-interactively** — returns before async work completes.

## 5. STATE
Builds and runs. `:app:testDebugUnitTest` BUILD SUCCESSFUL 31/31, all 55 corpus rows. Node gates 91/91. Rules emulator 25/25 (run before parser work). Installed debug on SM-S938U (`R5CY54B749F`), code 58, DEBUGGABLE. `chefvoice-billing` deployed; founding entitlement confirmed live. Nothing failing.

## 6. GOTCHAS
- `firebase functions:log` prints **newest first** — use `head`, not `tail`.
- `gradlew.bat` wraps PowerShell; `-i` fails as ambiguous with `-InformationAction`.
- `adb` is at `%LOCALAPPDATA%\Android\Sdk\platform-tools`, not on PATH.
- App is debuggable: `adb shell run-as com.chefvoice.app cat shared_prefs/chefvoice.xml` reads recipes/quota/analytics state.
- Second Pass quota increments **only on success** — a non-zero counter proves the cloud call returned.
- Samsung Freecess freezes the backgrounded app; `DNS ... isBlocked=true` in logcat is background restriction, not a network fault.
- Grant trigger fires on profile **create** only; existing accounts need `backfillChefVoiceLaunchAccess`.

## 7. NEXT
Add a corpus row for `"you're going to need your favorite Dorito chips, one bag"` — still lost. The determiner gate admits `some|a|an` but not `your`, and the quantity trails the name instead of leading it. Confirm red, then widen the gate without re-admitting `what you need to do`.

## 8. OPEN
- `firestore.rules` `config` block undeployed — diff local against live before deploying (a rules deploy replaces the whole ruleset).
- Analytics events never observed in DebugView; only local SharedPreferences bookkeeping verified.
- Whether the backfill granted all three accounts or only one.
- PWA half of the corpus contract cannot run (no `web/`); the 3 new rows are Android-verified only.
- `Mix it with taco.` still drops "seasoning"; the 5-minute rest step is still absent.
- Play Console Data safety form not updated (deletion URL + analytics collection).
