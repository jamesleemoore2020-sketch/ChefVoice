const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('Android reply model/repository is one-level and explicit',()=>{const m=read('app/src/main/java/com/chefvoice/app/model/Models.kt'),r=read('app/src/main/java/com/chefvoice/app/cloud/FirebaseSocialRepository.kt');assert.match(m,/parentCommentId: String/);assert.match(r,/fun addCommentReply/);assert.match(r,/"parentCommentId" to parent\.id/);});
test('Android renders replies nested and highlights notification target',()=>{const u=read('app/src/main/java/com/chefvoice/app/ui/ChefVoiceApp.kt'),s=read('app/src/main/java/com/chefvoice/app/ui/ChefAppState.kt');assert.match(u,/comments\.filter \{ it\.parentCommentId == comment\.id \}/);assert.match(u,/focusedCommentId == reply\.id/);assert.match(s,/"comment", "like", "reply"/);});
test('backend reply notification is deterministic and routes commentId',()=>{const f=read('notifications/functions/index.js');assert.match(f,/exports\.notifyCommentReply/);assert.match(f,/reply: "replies"/);assert.match(f,/commentId: cleanText\(notification\.commentId/);});
