const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('Android retains block UI and adds report repository',()=>{const u=read('app/src/main/java/com/chefvoice/app/ui/ChefVoiceApp.kt'),r=read('app/src/main/java/com/chefvoice/app/cloud/FirebaseSocialRepository.kt');assert.match(u,/Block chef/);assert.match(u,/Unblock/);assert.match(r,/fun submitReport/);});
test('Android report actions cover chef recipe comments replies and messages',()=>{const s=read('app/src/main/java/com/chefvoice/app/ui/ChefAppState.kt');for(const fn of ['reportChef','reportRecipe','reportRecipeComment','reportDirectMessage'])assert.match(s,new RegExp(`fun ${fn}`));});
test('report Firestore rules are reporter-private and immutable',()=>{const r=read('firestore.rules');assert.match(r,/match \/reports\/\{reportId\}/);assert.match(r,/reporterUid == request\.auth\.uid/);assert.match(r,/allow update, delete: if false/);});
